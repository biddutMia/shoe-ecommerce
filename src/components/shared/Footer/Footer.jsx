import { Container, Box } from "@mui/system";

const Footer = () => {
  return (
    <Box
      sx={{
        marginTop: "600px",
        background: "blue",
        color: "white",
        padding: "2px 0px",
      }}
    >
      <Container maxWidth="md">hello footer</Container>
    </Box>
  );
};

export default Footer;
